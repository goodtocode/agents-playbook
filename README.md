# Goodtocode.Agents.Playbook

Strongly typed Collect, Evaluate, and Record (CER) playbook contracts with a deterministic execution engine.

The core package is independent of Crucible, Microsoft Agent Framework, governance implementations, persistence, and transport formats. Hosts can execute the same typed playbook deterministically or adapt it to a workflow runtime.
